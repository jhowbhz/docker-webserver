# 🐳 Simple Dockerfile 
*****

### up docker image php7.4-fpm and nginx

> Run in background

```bash
    docker-compose up -d
```

> Run live

```bash
    docker-compose up -d
```

### show in browser

```bash
    docker-compose up
```
